services:
  app:
    build: .
    env_file: .env
    environment:
      - DB_PATH=/data/db.sqlite3
    ports:
      - "80:80"
    volumes:
      - sqlite_data:/data
      - media_data:/app/media
    restart: unless-stopped

volumes:
  sqlite_data:
  media_data:
